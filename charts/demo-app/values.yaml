replicaCount: 1

image:
  repository: nginx
  tag: 1.27-alpine
  pullPolicy: IfNotPresent

containerPort: 80

service:
  port: 80

ingress:
  enabled: true
  className: nginx
  host: app.localtest.me

probes:
  path: /

monitoring:
  serviceMonitor:
    enabled: true
    path: /metrics
